# apt packages installed on every machine (core tier).
# gopass comes from its own apt repo (registered by bootstrap.sh via
# add_apt_repo) because Ubuntu 26.04 dropped it from universe.
ca-certificates
curl
git
gopass
zsh
